# Wazuh Secrets

This directory is intentionally excluded from Git.

Store local-only Wazuh secret values here:

- `indexer_password.txt`
- `api_password.txt`
- `dashboard_password.txt`

These files are used by the local compose wrappers and private credential export.
