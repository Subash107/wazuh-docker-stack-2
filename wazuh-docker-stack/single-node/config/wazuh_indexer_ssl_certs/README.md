# Wazuh Indexer SSL Certificates

This directory is intentionally excluded from Git because it contains deployment-specific TLS material.

Place the generated certificate bundle here on the target host before starting the Wazuh single-node stack.
