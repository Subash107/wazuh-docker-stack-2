Tracked indicator lists for lightweight SOC enrichment.

- `opencti-indicators-ip`
- `opencti-indicators-domain`
- `opencti-indicators-sha256`

These files are consumed by Wazuh CDB list matching and can be generated from OpenCTI exports with the Ubuntu SOC helper scripts under `projects/ubuntu-lightweight-soc/scripts/`.
