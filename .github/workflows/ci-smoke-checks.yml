name: CI Smoke Checks

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  monitoring-smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Run monitoring smoke checks
        run: bash ./scripts/linux/ci_smoke_checks.sh
